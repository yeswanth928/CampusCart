import 'package:flutter/material.dart';

// Pink Color
final ThemeData pinkTheme = ThemeData(
  primaryColor: Colors.pink, // Used for login page background color too.
  primarySwatch: Colors.pink,
  primaryIconTheme: IconThemeData(color: Colors.pink),
  backgroundColor: Colors.white,
  cursorColor: Colors.white,
  cardColor: Colors.white,
  canvasColor: Colors.white,
  appBarTheme: AppBarTheme(
      color: Colors.pink,
      textTheme: TextTheme(
          headline1: TextStyle(
              color: Colors.white), // headline1 is used for titles in app bar.
          headline6: TextStyle(
              color: Colors
                  .white)), //headline 6 here is used for all the text, hint colors of search),
      iconTheme: IconThemeData(
        color: Colors.white,
      )),
  floatingActionButtonTheme: FloatingActionButtonThemeData(
      backgroundColor: Colors.pink, foregroundColor: Colors.white),
  hintColor: Colors.white,
  errorColor: Colors.white,
  textTheme: TextTheme(
      headline1:
          TextStyle(color: Colors.white), // Used in the drawer for "Menu"
      headline6: TextStyle(color: Colors.pink),
      subtitle2: TextStyle(color: Colors.black87)),
  bottomSheetTheme: BottomSheetThemeData(
    backgroundColor: Colors.white,
    modalBackgroundColor: Colors.white,
  ),
  iconTheme: IconThemeData(color: Colors.pink),
  tabBarTheme: TabBarTheme(
    labelColor: Colors.pink,
    unselectedLabelColor: Colors.pink[400],
    labelPadding: EdgeInsets.zero,
  ),
  splashColor: Colors.pink,
);

// Cyan Color
final ThemeData cyanTheme = ThemeData(
  primaryColor: Colors.cyan, // Used for login page background color too.
  primarySwatch: Colors.cyan,
  primaryIconTheme: IconThemeData(color: Colors.cyan),
  backgroundColor: Colors.white,
  cursorColor: Colors.white,
  cardColor: Colors.white,
  canvasColor: Colors.white,
  appBarTheme: AppBarTheme(
      color: Colors.cyan,
      textTheme: TextTheme(
          headline1: TextStyle(
              color: Colors.white), // headline1 is used for titles in app bar.
          headline6: TextStyle(
              color: Colors
                  .white)), //headline 6 here is used for all the text, hint colors of search),
      iconTheme: IconThemeData(
        color: Colors.white,
      )),
  floatingActionButtonTheme: FloatingActionButtonThemeData(
      backgroundColor: Colors.cyan, foregroundColor: Colors.white),
  hintColor: Colors.white,
  errorColor: Colors.white,
  textTheme: TextTheme(
      headline1:
          TextStyle(color: Colors.white), // Used in the drawer for "Menu"
      headline6: TextStyle(color: Colors.cyan),
      subtitle2: TextStyle(color: Colors.black87)),
  bottomSheetTheme: BottomSheetThemeData(
    backgroundColor: Colors.white,
    modalBackgroundColor: Colors.white,
  ),
  iconTheme: IconThemeData(color: Colors.cyan),
  tabBarTheme: TabBarTheme(
    labelColor: Colors.cyan,
    unselectedLabelColor: Colors.cyanAccent[100],
    labelPadding: EdgeInsets.zero,
  ),
  splashColor: Colors.cyan,
);

final ThemeData greenTheme = ThemeData(
  primaryColor:
      Colors.greenAccent[700], // Used for login page background color too.
  primarySwatch: Colors.green,
  primaryIconTheme: IconThemeData(color: Colors.greenAccent[700]),
  backgroundColor: Colors.white,
  cursorColor: Colors.white,
  cardColor: Colors.white,
  canvasColor: Colors.white,
  appBarTheme: AppBarTheme(
      color: Colors.greenAccent[700],
      textTheme: TextTheme(
          headline1: TextStyle(
              color: Colors.white), // headline1 is used for titles in app bar.
          headline6: TextStyle(
              color: Colors
                  .white)), //headline 6 here is used for all the text, hint colors of search),
      iconTheme: IconThemeData(
        color: Colors.white,
      )),
  floatingActionButtonTheme: FloatingActionButtonThemeData(
      backgroundColor: Colors.greenAccent[700], foregroundColor: Colors.white),
  hintColor: Colors.white,
  errorColor: Colors.white,
  textTheme: TextTheme(
      headline1:
          TextStyle(color: Colors.white), // Used in the drawer for "Menu"
      headline6: TextStyle(color: Colors.greenAccent[700]),
      subtitle2: TextStyle(color: Colors.black87)),
  bottomSheetTheme: BottomSheetThemeData(
    backgroundColor: Colors.white,
    modalBackgroundColor: Colors.white,
  ),
  iconTheme: IconThemeData(color: Colors.greenAccent[700]),
  tabBarTheme: TabBarTheme(
    labelColor: Colors.greenAccent[700],
    unselectedLabelColor: Colors.greenAccent[400],
    labelPadding: EdgeInsets.zero,
  ),
  splashColor: Colors.greenAccent[700],
);

final ThemeData redTheme = ThemeData(
  primaryColor: Colors.redAccent, // Used for login page background color too.
  primarySwatch: Colors.red,
  primaryIconTheme: IconThemeData(color: Colors.redAccent),
  backgroundColor: Colors.white,
  cursorColor: Colors.white,
  cardColor: Colors.white,
  canvasColor: Colors.white,
  appBarTheme: AppBarTheme(
      color: Colors.redAccent,
      textTheme: TextTheme(
          headline1: TextStyle(
              color: Colors.white), // headline1 is used for titles in app bar.
          headline6: TextStyle(
              color: Colors
                  .white)), //headline 6 here is used for all the text, hint colors of search),
      iconTheme: IconThemeData(
        color: Colors.white,
      )),
  floatingActionButtonTheme: FloatingActionButtonThemeData(
      backgroundColor: Colors.redAccent, foregroundColor: Colors.white),
  hintColor: Colors.white,
  errorColor: Colors.white,
  textTheme: TextTheme(
      headline1:
          TextStyle(color: Colors.white), // Used in the drawer for "Menu"
      headline6: TextStyle(color: Colors.redAccent),
      subtitle2: TextStyle(color: Colors.black87)),
  bottomSheetTheme: BottomSheetThemeData(
    backgroundColor: Colors.white,
    modalBackgroundColor: Colors.white,
  ),
  iconTheme: IconThemeData(color: Colors.redAccent),
  tabBarTheme: TabBarTheme(
    labelColor: Colors.redAccent,
    unselectedLabelColor: Colors.redAccent[100],
    labelPadding: EdgeInsets.zero,
  ),
  splashColor: Colors.redAccent,
);

final ThemeData yellowTheme = ThemeData(
  primaryColor:
      Colors.yellowAccent, // Used for login page background color too.
  primarySwatch: Colors.yellow,
  primaryIconTheme: IconThemeData(color: Colors.yellowAccent),
  backgroundColor: Colors.white,
  cursorColor: Colors.white,
  cardColor: Colors.white,
  canvasColor: Colors.white,
  appBarTheme: AppBarTheme(
      color: Colors.yellowAccent,
      textTheme: TextTheme(
          headline1: TextStyle(
              color: Colors.white), // headline1 is used for titles in app bar.
          headline6: TextStyle(
              color: Colors
                  .white)), //headline 6 here is used for all the text, hint colors of search),
      iconTheme: IconThemeData(
        color: Colors.white,
      )),
  floatingActionButtonTheme: FloatingActionButtonThemeData(
      backgroundColor: Colors.yellowAccent, foregroundColor: Colors.white),
  hintColor: Colors.white,
  errorColor: Colors.white,
  textTheme: TextTheme(
      headline1:
          TextStyle(color: Colors.white), // Used in the drawer for "Menu"
      headline6: TextStyle(color: Colors.yellow),
      subtitle2: TextStyle(color: Colors.black87)),
  bottomSheetTheme: BottomSheetThemeData(
    backgroundColor: Colors.white,
    modalBackgroundColor: Colors.white,
  ),
  iconTheme: IconThemeData(color: Colors.yellowAccent),
  tabBarTheme: TabBarTheme(
    labelColor: Colors.yellowAccent,
    unselectedLabelColor: Colors.yellowAccent[300],
    labelPadding: EdgeInsets.zero,
  ),
  splashColor: Colors.yellowAccent,
);
